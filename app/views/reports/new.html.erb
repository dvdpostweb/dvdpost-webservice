<% statuses = DVDPost.product_dvd_statuses.keys %>
<div id="addwishlist_title">
  <%= t '.title' %>
</div>
<ul>
  <% statuses.each do |event| %>
    <li>
      <%= link_to_report(t(".#{event}"), @order.to_param, event) %>
    </li>
  <% end %>
</ul>
