votre abo actuelle <%= current_customer.credit_per_month %>dvd par mois<br />
price <%= current_customer.price_per_month %><br />
date de reconduction => <%= current_customer.subscription_expiration_date.strftime("%d/%m/%Y") if current_customer.subscription_expiration_date %>
<table>
 <%= render :partial => 'reconductions/edit/abo', :collection => @list_abo, :locals => {:current_abo_credit => current_customer.credit_per_month, :selected_abo => current_customer.abo_type_id} %>
</table>
<% form_for(:customer, :url => customer_subscription_path(:customer_id => current_customer.to_param), :id => :form_account, :html => {:method => :put}) do |f| %>
  <%= f.hidden_field :next_abo_type_id, :value => 22 %>
  <div align='center' style="width:920px; height:50px"><button type="submit" class="button_confirm"><%= t '.confirm' %></button></div>
<% end %>