<div class="<%= product_media_id(transitted_item.product.media) %> clearfix">
  <div class="title"><%= link_to transitted_item.product.title, product_path(:id => transitted_item.product.to_param) %></div>
  <div class="status">
    <a href="#">Signaler un problème</a>
    <span class="ok"><%= "Expédié le #{transitted_item.changed_at.strftime("%d/%m/%Y")}" %></span>
  </div>
</div>
