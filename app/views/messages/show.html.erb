<div class="modal-window-content">
  <h3>
    <span><% @message.created_at.strftime("%d/%m/%Y") %></span>
    <%= h @message.category.name if @message.category %>
    <%= " : #{h @message.product.description.title}" if @message.product %>
  </h3>
  <div class="content">
    <h4>
      <%= t '.title' %>
    </h4>
    <%= @message.question %>
    <% if @message.updated_at %>
      <h4><%= t '.answer'%> <%= @message.updated_at.strftime("%d/%m/%Y") %></h4>
      <%=  @message.response %>
    <% end %>
  </div>
</div>
