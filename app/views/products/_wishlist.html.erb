<% if current_customer.wishlist_products.include?(product) %>
  Already in your wishlist
<% else %>
  <% text = title_add_to_wishlist(text) %>
  <%= link_to text, new_product_wishlist_item_path(:product_id => product.to_param), :class => 'btn add_to_wishlist_button' %>
<% end %>
