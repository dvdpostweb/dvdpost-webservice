<% if @token %>
  <%= link_to t('.replay'), streaming_product_path(:id => product.imdb_id), :class => "streaming_btn_replay #{I18n.locale}" %>
<% else %>
  <%= link_to t('.buy'), new_product_token_path(:product_id => product.to_param), :class => 'streaming_btn streaming_action' %>
<% end %>